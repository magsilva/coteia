Os arquivos envolvidoa quando desejando fazer alterações - adicionar funcionalidades - na CoTeia aparentemente são os create.php e o coteia.xsl. Na verdade, o create.php é que invoca o coteia.xsl nos dados da wiki sendo editada.

atualiza.php
Alterar a chamada para executar a transformação XST (xml_xsl). Basicamente tem de passar os parâmetros extras para executar a transformação.

create.php
Adicionar as novas tags:

+   if (stristr($conteudo,"<drnote/>")) {
+     $conteudo = drnote($conteudo);
+   }

edit.php
Adicionar o código relativo ao tratamento de novas tags.
+                if (stristr($conteudo,"<drnote/>")) {
+                        $conteudo = drnote($conteudo, $id_issue);
+                }

function.php.inc
Funções auxiliares por ventura utilizadas.


Todo:
- Ver as diferenças entre a CoTeia original (tag v1_0_0) e a CoTeia modificada da Simone
- Alterar a chamada xml_xsl para receber os parâmetros a serem aplicados via uma array associativa (key->value).
